# Windows / macOS 桌面体验实施计划

日期：2026-09-16。分支：`codex/desktop-experience`。

本轮 goal：普通用户双击打开 PLCN，选择设备并预览修复；配置不依赖安装目录，能够管理数据更新、任务和列表恢复。沿用 Python 核心与现有浏览器界面。

| 阶段 | 交付 | 验收 | 状态 |
| --- | --- | --- | --- |
| 0 基础成果 | 上轮安全写回、数据包和图片解析独立提交；创建草稿 PR | 保留历史，PR 可审阅 | 已完成 |
| 1 跨平台路径 | 资源只读、用户数据/缓存分离、旧配置及人工规则迁移 | 从任意工作目录启动；升级不覆盖用户配置；迁移幂等 | 已完成 |
| 2 启动与使用 | 桌面无控制台入口、单实例、明确退出、系统选择器、首次引导/继续上次目录、收起高级选项 | 双击重复启动复用服务；任务运行时不误退出；目录选择可退回内置浏览 | 已完成 |
| 3 数据与任务 | 数据包界面：检查/获取/比较/启用/回滚；任务取消与失败图片重试；本地写回历史和恢复 | 更新不自动启用；取消保留已完成步骤；恢复前检查外部变化并备份当前文件 | 已完成 |
| 4 平台交付 | Windows 安装包、macOS app/dmg、明确架构、签名配置、测试/文档、PR 完成 | 本机测试与 Windows 打包冒烟；远端 CI 检查；签名和实机边界明确 | 已完成 |

安全边界：取消为协作取消，不中断原子写回/远端替换；恢复首先支持本地列表，ADB 备份仍保留在设备并提供说明。关闭浏览器标签不等于停止服务，界面提供退出入口。数据包更新不执行下载来的程序。不会自动合并 PR 或发布 Release。

macOS 签名、公证需要用户自己的 Developer ID 与 Apple 凭据；Windows 签名需要证书。代码与 CI 可准备流程，缺少凭据时不得声称产物已签名或正式分发就绪。无 macOS 本机时以 CI 构建和说明为界，仍保留人工 Gatekeeper/安装/文件选择验收。

## 验证记录

- 基础提交：`0913593`；上轮 Windows 回归 102 passed / 1 skipped。

## 使用和后续

完成后在此记录启动、数据目录、打包产物、测试和 PR 链接。

## 本轮交付记录

- PR：[MightyKartz/PLCN#5](https://github.com/MightyKartz/PLCN/pull/5)，分支 `codex/desktop-experience`。基础成果与桌面改造分开提交。
- 路径：`app_paths.py` 分离资源、用户数据和缓存；复制迁移旧配置与人工规则，保留原件并防止重复覆盖。`PLCN_HOME` 用于隔离测试或显式便携配置。
- 启动：`desktop.py` 无控制台入口；OS 自动释放的实例锁、已有实例校验/复用、日志、退出时任务检查；系统选择器在 helper 主线程运行，失败回退内置浏览。
- 操作：首次目录入口、继续上次目录、收起高级配置、三种处理模式；仅图片模式保留列表字节和原 label。
- 管理：数据包检查/下载/比较/启用/回滚；任务取消与本地图片重试；持久化写回收据、外部修改拒绝、恢复前再次备份。ADB 仍保持远端备份和手动恢复边界。
- 验证：新增路径、迁移、实例锁、恢复、仅图片、取消、API、数据比较与启动流回归；本地完整回归 113 passed / 1 skipped（旧 Snatcher 样本缺数据）；最终提交的远端结果见 PR Checks。
- 已实际通过 Windows 安装包/便携包、macOS Intel 与 Apple Silicon 的 CI 构建及隔离目录打包自检；新增保留 Linux CLI 的打包任务。最新提交检查见 PR Checks。
- 本地 Windows 便携 ZIP 已生成于 `dist/PLCN-Windows-x64-Portable.zip`，仅供未签名预览。
- 浏览器冒烟：查询上游版本成功；仅名称修复摘要明确跳过下载；完成写回并生成恢复记录；实际恢复到原始 label，恢复前备份存在；重复启动返回同一 URL。数据包启用和回滚界面均验证；页面内确认框用于数据切换与恢复。
- Python 编译、内联与外部 JS 语法检查、Git 差异检查已加入验收。

签名证书、公证凭据没有配置；CI 自检不是 macOS Gatekeeper、原生文件选择器交互、升级安装或真实 ADB 的人工验收。上述项目继续作为正式发布门槛，不阻止代码 PR 的审阅。本轮没有合并 PR，也没有发布 Release。

使用说明与签名步骤：[DESKTOP_GUIDE.md](DESKTOP_GUIDE.md)。
